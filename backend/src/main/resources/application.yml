server:
  port: 8080

spring:
  application:
    name: money-buddy-api

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true
